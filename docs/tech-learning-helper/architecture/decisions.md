# 결정 기록

## 결정 기록 표

| ID | 결정 | 근거 | 기각한 대안 | 상태 | 도입 버전 | 관련 영역 | 대체한 결정 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AD-001 | 기술 지식을 요청 시점에 만들고, 처음 만든 개념·관계·예제를 저장해 다시 씁니다 | 어떤 기술이든 곧바로 학습하면서 이전 세션 이어 하기와 여러 기술에 같은 방식 적용에 필요한 일관된 기준을 남깁니다 | 매번 새로 생성: 같은 기술의 개념 구성이 바뀌어 이어 하기 기준이 흔들림. 주요 기술 사전 준비: 준비한 지식의 유지와 두 경로 관리 비용 | 채택 | 1.0 | 기술 지식 | |
| AD-002 | 가르치는 방법과 기술 지식을 나눕니다. 개념·관계·선수 지식은 기술 단위로, 탐색 질문은 예제마다 저장하고, 방법에는 기술과 무관한 규칙만 둡니다 | 예제를 벗어난 질문이 구조상 생기지 않고, 기술이 달라도 같은 학습 방식을 적용합니다 | 질문을 기술 단위로 모음: 예제를 벗어난 질문이 섞여 따로 검사해야 함. 분리하지 않음: 기술마다 방식이 달라져 같은 학습 방식을 적용하기 어려움 | 대체됨 | 1.0 | 학습 진행, 기술 지식 | AD-014 |
| AD-003 | 예제는 학습자 프로젝트와 분리된 학습 폴더에 두고, 기술마다 git 저장소 하나를 만들어 그 안을 학습 주제별 패키지로 나눕니다 | 기술별로 git 이력과 언어 도구가 나뉘고, 패키지 단위로 따로 실행할 수 있습니다 | 저장소 하나에 모든 기술: 여러 기술의 이력과 언어 도구가 한 저장소에 섞임. 패키지마다 완전 독립: 같은 의존성을 패키지마다 다시 설치함 | 채택 | 1.0 | 학습 작업 공간 | |
| AD-004 | 개념 사이에는 선수 관계만 두고, 탐색 질문마다 현상에서 원인과 개념으로 내려가는 설명 경로를 한 줄로 저장합니다. 개념과 선수 관계는 기술 저장소에, 질문과 설명 경로는 학습 패키지에 둡니다 | 질문 하나의 설명이 옆 개념으로 새지 않고 선형으로 진행되며, 지식 변경 이력이 예제와 같은 git 이력에 남습니다 | 관계 미저장: 같은 질문에도 경로가 매번 달라짐. 선수 관계만: 원인 경로가 매번 달라짐. 세 종류 관계 그래프: 연관 관계를 따라 설명이 오가게 됨 | 대체됨 | 1.0 | 기술 지식, 학습 진행 | AD-014 |
| AD-005 | 학습 상태는 기술 저장소에 파일 하나로 두고, 질의응답은 학습 패키지 안에 날짜별 파일로 남깁니다. 질문 하나의 설명 경로가 끝날 때마다 기술 저장소에 커밋합니다 | 주제는 폴더로, 날짜는 파일 이름으로 찾고, 기록이 다룬 예제 옆에 놓입니다. 새 세션도 파일에서 상태를 이어 받습니다 | 기술 저장소에 날짜별로 모음: 기록이 예제와 떨어져 글을 쓸 때 예제를 따로 찾아야 함. 커밋 이력만 사용: 긴 답변을 커밋 메시지로 읽어야 함 | 대체됨 | 1.0 | 학습 기록 | AD-013 |
| AD-006 | 사용자 스킬은 `learn`·`structure`·`trace`·`experiment`·`review` 다섯 개로 두고, `learn` 하나로 학습을 끝까지 진행하게 합니다. 나머지 넷은 탐색 방향을 바로 바꾸는 단축키입니다 | 학습자가 역할을 고르는 법을 따로 배우지 않아도 되고, 호출한 스킬의 지침이 세션 끝까지 남아 자연어 질문을 이어 받을 수 있습니다 | 내부 역할까지 모두 노출: 학습자가 어떤 스킬을 고를지부터 판단해야 함 | 채택 | 1.0 | 학습 진행 | |
| AD-007 | 내부 역할은 공용 참조 문서로 두고, 질문 유형에 따라 읽을 역할 문서를 `learn` 절차에 명시합니다. 전체 조율(Top-down Tutor)은 `learn`의 절차가 맡습니다 | 질문이 매번 같은 역할로 이어지고, 학습과 무관한 작업에서 역할이 불리지 않습니다 | 숨긴 스킬: 역할 선택을 Claude의 판단에 맡겨 학습 밖에서도 불릴 수 있음. 서브에이전트: 대화를 보지 못해 대화로 진행하는 역할에 맞지 않음 | 채택 | 1.0 | 학습 진행, 기술 지식, 학습 작업 공간, 학습 기록 | |
| AD-008 | 학습 폴더 위치는 플러그인 `userConfig`의 `directory` 항목으로 플러그인을 켤 때 학습자에게 받고, 모든 스킬이 `${user_config.<키>}`로 읽습니다 | 학습자가 직접 위치를 정하고, 다섯 스킬이 공식 기능 하나로 같은 위치를 찾습니다 | 첫 `learn`에서 묻고 플러그인 데이터 폴더에 저장: 설정 파일 규칙을 따로 만들어야 하고 재설치 때 다시 물음. 환경 변수: 학습자가 설치 안내를 읽고 직접 설정해야 함 | 대체됨 | 1.0 | 학습 작업 공간 | AD-011 |
| AD-009 | 가져온 자료는 구간의 동작을 결정하는 기술 중 가장 좁은 기술의 저장소에 넣습니다. 판정 결과를 학습자에게 보여 확인받고, 비슷한 이름의 저장소가 있으면 그 저장소를 제안합니다 | 기술의 근간을 기준으로 저장소가 정해지고, 잘못된 판정을 학습자가 바로잡으며, 저장소가 겹쳐 생기지 않습니다 | 판정 결과만 알림: 잘못 판정해도 학습자가 모른 채 넘어감. 질문 기준 판정: 같은 코드 구간이 질문마다 다른 저장소로 흩어짐 | 채택 | 1.0 | 학습 작업 공간 | |
| AD-010 | 가져온 자료는 원본 참조와 질문에 해당하는 발췌를 학습 패키지에 남깁니다. 링크 같은 공개 자료는 원본 구간을 옮겨 부족한 부분만 채우고, 로컬 파일과 출처를 알 수 없는 붙여 넣은 코드는 질문한 동작만 남긴 최소 실행 예제로 줄입니다. 원본을 쓸 수 없으면 같은 동작의 예제를 새로 만듭니다 | 원본을 기반으로 배우면서, 회사 프로젝트 코드가 학습 저장소의 git 이력에 그대로 남지 않습니다 | 모든 원본을 그대로 옮김: 회사 코드가 git 이력에 남음. 모든 원본을 최소 예제로 줄임: 공개 자료도 원본과 줄 단위로 대응하지 않음 | 채택 | 1.0 | 기술 지식, 학습 작업 공간 | |
| AD-011 | 학습 폴더 위치는 첫 `learn`에서 학습자에게 묻고, 홈 디렉터리 아래 도구와 무관한 고정 설정 파일에 저장합니다. 모든 스킬이 이 파일에서 위치를 읽습니다 | Codex 플러그인은 `userConfig` 같은 사용자 설정을 지원하지 않습니다. 설정 파일 하나로 Claude Code와 Codex가 같은 학습 폴더를 씁니다 | 도구마다 다르게 받음: 위치를 읽는 방법이 둘로 나뉘고 두 도구의 학습 폴더가 달라질 수 있음. 환경 변수: 학습자가 설치 안내를 읽고 직접 설정해야 함 | 채택 | 2.0 | 학습 작업 공간, 학습 진행 | |
| AD-012 | 누적 학습 상태는 `state.md`에, 중간 세션 재개 상태는 기술 저장소 루트의 `state.json`에 둡니다 | 경로 중단 시점과 장기 학습 상태를 분리해 새 세션이 기록 전문을 추정하지 않게 합니다 | `state.md` 하나에 함께 저장: 마크다운 누적 상태와 세션 입력 상태의 갱신 주기가 다름 | 대체됨 | 2.3 | 학습 기록, 학습 진행 | AD-016 |
| AD-013 | 질의응답은 학습 패키지의 `records/YYYY-MM-DD/01-question-slug.md`에 질문별로 남기고, 한 질문의 설명 경로와 실험을 같은 파일에 기록합니다. 기존 `records/YYYY-MM-DD.md`는 읽기 호환 대상으로만 처리합니다 | 질문별 파일만 읽어 현재 맥락을 복원하고, 날짜 디렉터리로 기록을 찾으며, 기존 학습 저장소도 계속 읽을 수 있습니다 | 날짜별 단일 파일 유지: 질문 수가 늘수록 읽어야 할 맥락이 커짐. 질문마다 실험 파일 분리: 같은 질문의 설명과 확인 과정이 흩어짐 | 채택 | 2.4 | 학습 기록, 학습 진행 | |
| AD-014 | 가르치는 방법과 기술 지식은 나누고, 방법에는 기술과 무관한 규칙만 둡니다. 탐색 질문마다 현상에서 원인과 개념으로 내려가는 설명 경로를 한 줄로 저장하고, 질문과 설명 경로는 학습 패키지에 둡니다. 개념은 별도 목록으로 저장하지 않고 설명 경로의 서술 안에만 두며, 선수 개념이 필요하면 경로의 한 단계로 넣습니다. 다른 패키지에서 같은 개념을 다시 설명하지 않도록, 저장소 루트 상태 파일에 모은 발견한 개념 이름 목록을 확인합니다 | 개념 정의를 별도 파일로 관리하는 비용 없이도, 설명 경로 서술과 발견한 개념 이름 목록만으로 같은 개념의 중복 설명을 막을 수 있습니다 | 개념·선수 관계를 기술 저장소의 별도 파일에 저장(AD-004의 기존 방식): 개념 유무가 설명 경로와 별도 파일에 중복되어 갱신 지점이 늘어남. 패키지 상태 파일마다 개념의 뜻을 저장: 같은 개념의 뜻이 패키지마다 다르게 적힐 위험 | 채택 | 3.0 | 기술 지식, 학습 진행, 학습 기록 | |
| AD-015 | `review`는 `summary.md` 같은 새 파일을 만들지 않고, 진행 중이거나 고른 패키지의 상태 파일 값을 그대로 옮겨 채팅으로 보여 줍니다 | 패키지 상태 파일이 이미 누적 학습 상태를 담고 있어, 같은 값을 별도 파일에 다시 옮기면 두 파일의 갱신 시점이 어긋날 수 있습니다 | `summary.md` 유지: 패키지 상태 파일과 값이 중복되어 둘 중 하나가 먼저 갱신되면 나머지가 낡음. `review` 스킬 제거: 학습자가 상태 파일을 직접 읽어야 해 가독성이 떨어짐 | 채택 | 3.0 | 학습 기록 | |
| AD-016 | 학습 상태는 패키지 단위로 파일 하나에 담습니다. 이 파일은 그 패키지의 세션 재개 상태(현재 질문·공개 단계·다음 기대 입력)와 누적 학습 상태(질문별 진행 상태·발견한 개념·부분 이해 개념·다음 탐색 후보)를 모두 포함합니다. 저장소 루트에는 별도 파일을 두어 현재 진행 중인 패키지, 마지막 학습 패키지, 발견한 개념 이름 목록(패키지 무관하게 모음)만 담습니다 | 패키지가 여러 개인 저장소에서도 진행 상태를 패키지 단위로 정확히 구분하고, 새 세션이 저장소 루트만 보고 진행 중인 패키지를 알 수 있습니다. 같은 정보를 두 파일에 중복해서 담지 않습니다 | 저장소 전체에 상태 파일 하나만 유지: 패키지가 여러 개면 어느 패키지의 상태인지 구분할 수 없음. 패키지마다 세션 상태와 누적 상태를 각각 다른 파일로 나눔: 같은 패키지의 상태 정보가 두 파일에 나뉘어 갱신 시점이 어긋날 위험 | 대체됨 | 3.0 | 학습 기록, 학습 진행 | AD-017 |
| AD-018 | 새 질문은 생소한 주제로 보고, 현재 안내 범위 밖의 개념은 설명하지 않고 다음 탐색 후보로 보류합니다 | 사전 지식을 묻는 부담을 없애고, 한 번에 다루는 개념 범위를 유지해 혼란을 줄입니다 | 학습자의 사전 지식을 먼저 진단: 진단 질문 자체가 부담이 되고 판정이 부정확함. 범위 밖 개념까지 함께 설명: 개념 지도가 넓어져 현재 질문의 이해가 어려워짐 | 채택 | 5.0 | 학습 진행 | |
| AD-019 | 새 질문의 첫 관찰 전에 범위·관계 지도·용어별 한 줄 설명·관찰 대상을 안내합니다 | 생소한 주제의 구조와 관찰 방향을 먼저 제공하면 추측을 요구해도 질문의 의미를 이해할 수 있습니다 | 관찰부터 시작: 선수 지식이 없으면 질문 의도를 이해하기 어려움. 전체 설명부터 제공: 학습자가 직접 관찰하고 추론할 여지가 줄어듦 | 대체됨 | 5.0 | 학습 진행 | AD-024 |
| AD-020 | 사전 안내 뒤에는 별도 이해 확인 없이 첫 관찰 유도로 바로 넘어갑니다 | 사전 안내를 추가 시험으로 만들지 않아 학습자의 부담을 줄이고 탐색 흐름을 유지합니다 | 관계 지도 재진술 요구: 안내가 확인 문제로 바뀜. 질문을 먼저 받음: 학습자가 범위 밖 개념으로 이동할 수 있음 | 채택 | 5.0 | 학습 진행 | |
| AD-021 | 사전 안내는 질문 항목의 `orientation` 필드에 범위·관계 지도·용어별 한 줄 설명·관찰 대상으로 저장합니다 | 같은 질문을 다시 학습할 때 진입 안내를 재사용하고 설명 경로와 책임을 분리할 수 있습니다 | 설명 경로의 첫 단계에 저장: 안내와 학습 단계가 섞임. 매번 생성: 같은 질문의 안내가 달라질 수 있음 | 대체됨 | 5.0 | 기술 지식, 학습 진행 | AD-025 |
| AD-022 | `orientation`에는 현재 설명 경로에 필요한 개념만 담습니다 | 질문마다 필요한 범위를 좁혀 기술 전체의 개념 지도가 학습을 방해하지 않게 합니다 | 기술 전체를 포함: 현재 질문과 관계없는 개념이 섞임. 학습자에게 범위를 지정하게 함: 시작 전에 추가 판단 부담이 생김 | 대체됨 | 5.0 | 기술 지식, 학습 진행 | AD-023 |
| AD-017 | 저장소 루트 파일에는 패키지마다 이름과 마지막 활동 날짜만 모은 목록과, 발견한 개념 이름 목록만 담습니다. 진행 중인 패키지를 가리키는 포인터나 세션·입력 상태는 두지 않습니다. 새 세션은 이 포인터로 곧바로 재개하지 않고, 항상 이어 하기 목록에서 패키지를 고르게 한 뒤 그 패키지 파일의 세션 재개 상태로 이어갑니다 | 진행 중인 패키지를 가리키는 포인터를 두면, 학습자가 설명 경로를 끝내지 않고 다른 기술로 넘어갔을 때 그 값을 정리할 절차가 없어 낡은 채로 남습니다. 마지막 활동 날짜라는 사실만 기록하면 손대지 않은 패키지는 그대로 둬도 됩니다. 패키지를 고른 뒤의 입력 해석은 그 패키지 파일의 `awaiting` 필드로 이미 판단할 수 있어, 저장소 파일에 별도의 입력 모드가 필요 없습니다 | 진행 중인 패키지 포인터와 세션·입력 상태를 저장소 파일에 둠(AD-016의 기존 방식): 다른 저장소로 전환해도 정리되지 않는 포인터가 남고, 새 세션이 이어 하기 목록 없이 곧바로 예전 질문으로 들어가 학습자가 다른 것을 배우고 싶어도 확인 없이 이어짐. 저장소 파일에 아무 인덱스도 두지 않음: 이어 하기 목록을 보여줄 때마다 모든 패키지의 `records/`를 스캔해야 함 | 채택 | 4.0 | 학습 기록, 학습 진행 | |
| AD-023 | 사전 안내의 범위는 학습자가 실행한 예제에서 관찰할 수 있는 것으로 정합니다. 예제에서 드러나는 값·로그·호출은 모두 안내 재료로 쓰고, 학습자 환경에서 재현되지 않는 다른 환경의 상황은 넣지 않습니다 | 개념 목록으로 범위를 자르면 예제에 보이는데도 설명되지 않는 요소가 남습니다. 학습자가 보는 화면을 기준으로 삼으면 안내와 관찰 대상이 어긋나지 않습니다 | 설명 경로에 필요한 개념만 담고 깊이만 늘림: 예제에 보이는데 경로에 없는 요소가 계속 설명되지 않음. 경로 밖 요소를 관찰 대상에만 올림: 이름만 보이고 하는 일을 알 수 없음 | 채택 | 6.0 | 기술 지식, 학습 진행 | |
| AD-024 | 사전 안내의 용어 설명은 정체·역할·구성 요소·사용 시점 순서로 씁니다. 이 순서는 규칙 문서가 정하고, 저장은 용어마다 설명 한 덩어리로 받습니다 | 한 줄 정의로는 학습자가 관찰 질문에 답할 근거를 얻지 못합니다. 네 항목을 저장 단위로 쪼개면 구성 요소가 없는 용어에서 억지 서술이 생깁니다 | 네 항목을 각각 필드로 저장하고 빈 항목을 거부: 용어 성격에 맞춰 늘이고 줄일 수 없음. 저장 단계에서 네 항목의 존재를 검사: 검사 기준을 따로 만들어야 함 | 채택 | 6.0 | 기술 지식, 학습 진행 | |
| AD-025 | 용어는 선행 관계가 앞에 오도록 배열해 저장하고, 개념 사이의 관계는 각 용어 설명 안에 함께 씁니다. 학습자에게 보여 줄 때는 저장된 순서를 그대로 따릅니다 | 관계를 따로 떼어 두면 같은 말이 두 번 나오면서도 용어가 하는 일은 어느 쪽에도 남지 않습니다. 저장 순서를 그대로 쓰면 같은 질문을 다시 열어도 순서가 유지됩니다 | 보여 줄 때마다 순서를 다시 판단: 같은 질문의 안내가 세션마다 달라짐. 용어마다 선행 용어를 함께 저장: 용어가 두세 개인 안내에서 유지 비용이 더 큼 | 채택 | 6.0 | 기술 지식, 학습 진행 | |
| AD-026 | 학습자가 되묻는 용어가 현재 사전 안내에 담겨 있으면 선행 지식 부족으로 보고, 요청을 기다리지 않고 그 용어부터 다시 설명합니다 | 되묻는 행위만으로는 선행 지식 부족을 판정할 수 없습니다. 저장된 안내를 기준으로 삼으면 판정이 사람마다 달라지지 않습니다 | 되묻기 자체를 신호로 봄: 안내 밖 개념을 향한 질문까지 설명 대상이 됨. 답변에 쓰인 용어를 관찰해 판정: 판정 기준이 흔들림 | 채택 | 6.0 | 학습 진행 | |
| AD-027 | 사전 안내에 작성 기준 표시를 함께 저장하고, 표시가 없는 안내는 그 질문을 다시 고를 때 한 번 다시 만듭니다 | 필드가 채워져 있다는 이유만으로 옛 기준의 안내가 계속 재사용됩니다. 표시를 남기면 갱신이 한 번으로 끝나고 이후에는 재사용됩니다 | 저장된 안내를 그대로 재사용: 옛 기준의 얕은 안내가 남음. 다시 고를 때마다 새로 만듦: 같은 질문의 안내와 순서가 매번 달라짐 | 채택 | 6.0 | 기술 지식 | |
