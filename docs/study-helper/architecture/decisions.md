# 결정 기록

## 결정 기록 표

| ID | 결정 | 근거 | 기각한 대안 | 상태 | 도입 버전 | 관련 영역 | 대체한 결정 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AD-001 | 사용자 스킬은 동작마다 하나씩 `study`·`hint`·`review` 세 개로 둡니다. `study [기술]`은 배울 범위를 구체화하고 과제를 내며, 같은 기술로 다른 과제를 받을 때도 `study`를 다시 부릅니다. `hint`는 힌트를, `review`는 완성된 예제의 피드백을 줍니다. 모든 요청은 해당 스킬을 명시적으로 불러야 합니다 | 동작을 명령 이름으로 구분해 새 세션에서도 명령 하나로 원하는 동작에 바로 들어갑니다. 사용자가 힌트를 부르는 순간이 명확해져 R5의 "요청하면"이 판정 가능해집니다 | `study` 하나로 대화 진행: 동작 구분을 AI의 대화 해석에 맡김. `study`와 동작별 스킬 병행: 같은 동작의 진입 경로가 둘이 됨. 다음 과제 전용 `next` 스킬: 과제를 내는 진입 경로가 둘이 되고, 현재 기술을 알아내는 방법이 따로 필요함 | 대체됨 | 1.0 | 학습 진행 | AD-006 |
| AD-002 | 학습 기조는 플러그인 소스의 공유 문서 하나에 원칙만 둡니다. 원칙을 과제·힌트·피드백에 적용하는 방법은 각 스킬 문서에 적습니다. 기조는 이 저장소에서 고쳐 커밋하고 플러그인을 갱신해 반영합니다 | 원칙과 적용 방법을 나누면 스킬마다 같은 원칙을 다르게 적용할 수 있습니다. 기조를 플러그인 소스에 두면 변경 이력이 커밋으로 남습니다 | 모든 스킬이 기조 문서 전체를 읽음: 원칙을 스킬마다 어떻게 적용할지 AI 판단에 맡김. 항목마다 적용 대상을 표시해 해당 항목만 읽음: 적용 방법은 여전히 AI 판단에 남음. 학습 폴더에 기조 파일을 둠: 사용자가 소스에서 고치는 방식과 맞지 않음 | 대체됨 | 1.0 | 학습 기조, 학습 진행 | AD-014 |
| AD-003 | 과제 폴더는 `study`를 부른 현재 디렉터리 바로 아래에 과제마다 하나씩 만들고, 과제 폴더마다 의존성을 따로 설치합니다 | 과제 폴더 하나만으로 실행할 수 있어 다른 과제의 설치 상태와 무관하게 R1을 충족합니다. 사용자가 과제 위치를 따로 설정하지 않습니다 | 학습 전용 폴더를 설정해 둠: 위치 설정 절차가 필요함. 기술 폴더 아래 과제 폴더를 두고 의존성 공유: 한 과제의 의존성 변경이 다른 과제에 영향을 줌 | 채택 | 1.0 | 학습 작업 공간 | |
| AD-004 | 과제 정보(기술, 만들 대상, 완성 조건)는 과제 폴더 안에 기록합니다. `hint`·`review`는 과제 폴더 안에서 불러 그 폴더의 과제 정보를 읽습니다. `study`는 현재 디렉터리의 과제 폴더들에서 과제 정보를 읽어, 같은 기술의 과제와 만들 대상이 겹치지 않는 과제를 냅니다 | 과제 정보가 과제 코드 옆에 있어 새 세션에서도 부른 위치만으로 대상 과제가 정해집니다. 정보를 한 곳에만 두어 갱신할 곳이 하나입니다 | 현재 디렉터리에 기술별 과제 목록 파일을 추가: 과제 폴더 기록과 목록 파일을 함께 갱신해야 함. 호출할 때마다 과제 목록에서 고름: 과제 폴더 안에서도 매번 선택 절차를 거침 | 대체됨 | 1.0 | 학습 진행, 학습 작업 공간 | AD-010 |
| AD-005 | `study`는 과제 폴더에 사용자가 설계와 구현 단계를 기록하는 `PLAN.md`를 함께 만듭니다. 구현 단계는 체크박스로 진행 상태를 표시하고, 막힌 점과 설계 변경도 기록합니다. 사용자가 내용을 쓰며 `hint`·`review`는 읽기만 합니다. 문서가 비어 있어도 두 스킬은 실행합니다 | 과제 설명과 사용자 작업 계획을 분리하면서, 힌트와 피드백이 현재 설계·진행 상태를 참고할 수 있습니다. 계획이 없어도 과제 수행을 막지 않아 기존 동작을 유지합니다 | `TASK.md`에 계획을 함께 기록: 과제 설명과 사용자 작업 기록이 섞임. `hint`·`review`가 계획을 수정: 사용자의 설계와 진행 판단을 대신함. 계획이 없으면 두 스킬을 중단: 기록 누락이 과제 진행을 막음 | 대체됨 | 1.1 | 학습 진행, 학습 작업 공간 | AD-007 |
| AD-006 | 사용자 스킬은 동작마다 하나씩 `study`·`tutor`·`hint`·`review` 네 개로 둡니다. `tutor`는 사용자가 다음에 할 일을 몰라 멈췄을 때 그 일을 하나만 정해 주고, `hint`는 정한 일을 푸는 방법에 막혔을 때 힌트를 줍니다. `tutor`가 정해 준 일에서 막히면 먼저 `hint`를 부르고, 같은 일에서 `tutor`를 다시 부르면 그 일을 더 작은 일로 쪼갭니다. 모든 요청은 해당 스킬을 명시적으로 불러야 합니다 | 사용자가 모르는 것이 무엇을 할지인지 어떻게 풀지인지에 따라 부르는 스킬이 갈려, 요청하지 않은 도움이 섞이지 않습니다. 다음 할 일을 하나로 제한하고 재호출 때 잘게 쪼개면 사용자가 한 번에 받는 정보가 스스로 진행할 만큼으로 유지됩니다(P1, P9) | `hint`가 다음 할 일까지 제시: 힌트를 요청한 적 없는데 힌트가 섞임. `tutor`가 힌트까지 제시: 두 스킬의 경계가 겹쳐 노력이 드는 지점을 건너뜀. 남은 할 일을 한꺼번에 제시: 사용자가 설계를 스스로 나누는 경험이 사라짐 | 대체됨 | 2.0 | 학습 진행 | AD-009 |
| AD-007 | `PLAN.md`는 사용자와 `tutor`가 함께 씁니다. `tutor`는 설계·구현 단계·막힌 점·설계 변경 네 절을 모두 고칠 수 있으며, 사용자가 이미 쓴 줄을 고칠 때는 고칠 곳과 이유를 먼저 제시하고 동의를 받습니다. `hint`·`review`는 읽기만 하고, 문서가 비어 있어도 실행합니다 | 다음 할 일이 문서에 남아야 다음 세션과 다른 스킬이 같은 진행 상태를 읽습니다. 기존 내용의 수정에 동의를 요구하면 설계의 결정권이 사용자에게 남습니다(P15) | `tutor`도 읽기만 하고 대화로만 제시: 제시한 할 일이 기록되지 않아 다음 호출 때 진행 상태를 다시 판정해야 함. `tutor`가 동의 없이 고침: 사용자가 쓴 설계가 예고 없이 바뀜. `tutor` 전용 절을 따로 둠: 같은 설계가 두 곳에 나뉘어 어느 쪽이 현재인지 모호해짐 | 대체됨 | 2.0 | 학습 진행, 학습 작업 공간 | AD-013 |
| AD-008 | `tutor`는 `PLAN.md`와 `TASK.md`와 과제 폴더의 코드를 읽고, 어디서 멈췄는지 사용자에게 항상 한 번 물은 뒤 멈춘 지점을 판정합니다 | 세 자료는 사용자가 어디까지 했는지는 보여 주지만 왜 멈췄는지는 보여 주지 않습니다. 확인 질문 하나가 그 차이를 메우고, 사용자가 자기 상태를 말로 옮기는 자리가 됩니다(P3, P15) | 세 자료만으로 판정: 비어 있는 `PLAN.md`나 방금 시작한 코드에서는 멈춘 이유를 알 수 없음. 판정이 갈릴 때만 질문: 판정이 갈렸는지 자체를 AI가 정해 질문이 생략될 수 있음. 질문만으로 판정: 사용자가 상황을 요약해야 하는 부담이 생기고 코드의 실제 진행과 어긋날 수 있음 | 대체됨 | 2.0 | 학습 진행 | AD-012 |
| AD-009 | 사용자 스킬은 `init`·`tutor`·`manager` 셋으로 둡니다. `init`은 예제와 환경을 만들고, `tutor`는 예제 구성의 방향을 정하며 진행 중 질문에 답하고, `manager`는 마친 예제의 기록과 복습을 맡습니다. `init`은 예제를 만들기 전에 `tutor` 문서의 수준 판정 절차를 읽어 자기 대화에서 수행하며 스킬을 전환하지 않습니다. 앞선 결정에 적힌 `study`는 `init`을 가리킵니다 | 사용자가 예제를 시작할 때 스킬을 두 번 부르지 않아도 되고, 수준 판정 절차는 `tutor` 문서 한 곳에만 남습니다. 세 스킬의 경계가 예제 생성·진행 중 도움·마친 뒤 관리로 갈려 요청하지 않은 도움이 섞이지 않습니다 | `init`이 `tutor`를 선행 실행하고 돌아옴: 사용자가 스킬 전환을 보게 되고 대화 맥락이 나뉨. 판정 절차를 공유 문서로 분리: 문서가 하나 늘고 `tutor` 문서만 읽어서는 판정 절차를 알 수 없음 | 대체됨 | 3.0 | 학습 진행 | AD-016 |
| AD-010 | 예제 정보(기술, 만들 대상, 완성 조건, 수준, 학습 목표, 뺀 기능과 이유)를 예제 폴더의 문서 한 편에 모두 적고 별도 JSON을 두지 않습니다 | 같은 값이 두 곳에 나뉘면 한쪽만 고쳤을 때 어긋납니다. 스킬이 문서를 읽으므로 값을 따로 둘 필요가 없습니다 | 값은 JSON, 설명은 문서: 수준과 학습 목표가 양쪽에 중복됨. 값은 JSON에만: 예제 문서에 수준과 학습 목표가 보여야 한다는 R3-2·R6-2와 어긋남 | 대체됨 | 3.0 | 학습 진행, 학습 작업 공간 | AD-017 |
| AD-011 | 예제마다 기록 파일 하나를 만들어 예제 폴더들과 같은 높이의 `.study/records/` 아래에 모읍니다. 기록에는 학습 목표, 배운 내용, 마친 날짜, 답변 단계 이력을 적습니다 | 예제 폴더를 열지 않고 기록 전체를 훑어 복습 대상을 고릅니다(R10-2, R11-1). 예제마다 파일을 나누면 특정 예제의 기록만 읽을 수 있습니다 | 파일 하나에 모두 모음: 예제가 쌓일수록 한 파일이 길어짐. 기록은 예제 폴더 안에 두고 색인만 상위에 둠: 복습 질문을 만들 때 예제 폴더를 열어야 해 R10-2와 어긋남 | 대체됨 | 3.0 | 학습 진행, 학습 작업 공간 | AD-020 |
| AD-012 | 답변 단계는 예제마다 하나를 기록 파일에 남깁니다. 1단계는 `tutor`를 부르기 전의 상태로 두고, 질문을 받으면 2단계에서 시작합니다. 사용자가 더 알려 달라고 할 때만 단계를 올리고, 앞 질문과 다른 내용이면 2단계로 되돌립니다 | 이미 시도하다 막혀서 물은 사용자에게 1단계 답변은 답이 되지 않습니다. 단계를 예제마다 하나만 두면 관리가 단순하고, 다른 질문에서 되돌리면 노력이 드는 지점을 건너뛰지 않습니다 | 질문마다 단계를 따로 기록: 여러 주제의 단계를 각각 유지해야 함. 예제별 단계를 새 질문에도 이어감: 앞 질문에서 올라간 단계가 새 질문에 그대로 적용됨. 대화 안에서만 유지: 새 세션에서 단계가 사라짐 | 대체됨 | 3.0 | 학습 진행 | AD-020 |
| AD-013 | `PLAN.md`는 사용자만 씁니다. `tutor`는 읽기만 하고 고치지 않으며, 문서가 비어 있어도 실행합니다 | `tutor`가 다음 할 일을 정하는 역할이 빠져 쓸 내용이 없습니다. 사용자가 적은 시도 기록은 답변 단계를 판단하는 자료가 됩니다 | `tutor`가 막힌 점 절에 답변 단계를 기록: 사용자가 쓰는 문서에 스킬의 기록이 섞임. 답변 단계를 `PLAN.md`에만 둠: `manager`가 단계 이력을 보려면 예제 폴더를 열어야 함 | 채택 | 3.0 | 학습 진행, 학습 작업 공간 |  |
| AD-014 | 학습 기조 문서를 두지 않고, 학습 원칙을 각 스킬 문서의 요구사항에 녹여 적습니다 | 원칙과 적용 규칙이 나뉘어 있으면 원칙을 고칠 때 적용 규칙을 모두 찾아 함께 고쳐야 합니다. 세 스킬의 요구사항이 이미 원칙을 판정 가능한 형태로 담습니다 | AD-002 유지: 세 스킬 문서의 적용 규칙과 기조 문서를 함께 갱신해야 함. `tutor`에만 둠: `init`과 `manager`의 결과가 원칙과 어긋날 때 판단 근거가 없음 | 채택 | 3.0 | 학습 진행 |  |
| AD-015 | `manager`는 다음 학습의 주제와 난이도를 제안만 하고, 사용자가 그 내용을 `init` 호출에 옮겨 씁니다 | 두 스킬 사이에 파일을 두지 않아 읽을 곳과 반영 시점을 정하는 절차가 필요 없습니다. 다음에 무엇을 할지는 사용자가 정합니다 | 기록 파일에 남기고 `init`이 최신 것을 읽음: 여러 기록 중 무엇이 최신인지 판정해야 함. 전용 파일에 남김: 파일이 하나 늘고 언제까지 반영되는지 모호해짐 | 채택 | 3.0 | 학습 진행 | |
| AD-016 | 예제를 만들기 전의 질문과 판단을 두 문서로 나눕니다. `init` 문서는 무엇을 어떤 순서로 묻고 어떻게 묶는지를 맡고, 사용자의 답을 모읍니다. `tutor` 문서의 `예제 구성 방향` 절은 학습 활동 후보 추천, 수준 판정, 학습 목표와 예제 범위를 정하는 기준을 맡습니다. `init`은 답을 모은 뒤 그 기준 절을 읽어 같은 대화에서 판단하며 스킬을 전환하지 않습니다 | 활동 추천의 재료는 대화 전체에 흩어진 사용자의 의도이므로 대화 맥락 안에서 판단해야 추천이 의도에 맞습니다. 사용자가 후보를 고쳐 달라고 하면 바로 반영합니다. 묻는 일과 판단 기준을 나누면 각 문서의 책임이 분명해집니다 | 서브에이전트로 `tutor`를 실행해 판단을 넘김: 요약에서 빠진 의도가 추천에 반영되지 않고 수정할 때마다 다시 호출해야 하며 Codex에서의 동작이 확인되지 않음. 답을 파일에 적고 사용자가 `tutor`를 부름: 스킬을 두 번 불러야 해 R3과 어긋남. 질문과 판단을 모두 `tutor` 절에 둠(AD-009): 질문 절차가 판단 기준과 섞여 `init`의 흐름이 `init` 문서에 드러나지 않음 | 채택 | 4.0 | 학습 진행 |  |
| AD-017 | 예제 정보(기술, 학습 활동, 시작 상태, 산출물, 확인 방법, 수준, 학습 목표, 완성 조건, 뺀 기능과 이유)를 예제 폴더의 문서 한 편에 모두 적고 별도 JSON을 두지 않습니다. `만들 대상`은 `학습 활동`으로 바꾸고, 예제 폴더 이름은 `<기술>-<학습 활동 요약>`으로 짓습니다. 앞선 예제와의 겹침은 같은 기술 안에서 산출물을 먼저 비교해, 산출물이 다르면 겹치지 않는다고 봅니다. 산출물이 같을 때만 활동이 요구하는 동작이 같은지 따집니다 | 같은 앱을 쓰더라도 남기는 산출물이 다르면 연습하는 일이 다릅니다. 산출물을 먼저 비교하면 판정이 값 비교로 끝나는 경우가 늘고, 산출물이 같은 만들기 과제끼리는 기존 동작 비교로 가립니다. 값을 문서 한 편에 두는 이유는 AD-010과 같습니다 | 활동 문장의 동작만 비교: 산출물의 차이가 판정에 명시적으로 들어가지 않음. 시작 상태·산출물·확인 방법 세 값이 모두 같으면 겹침: 만들기 과제는 대부분 세 값이 같아 서로 다른 과제도 겹친다고 판정함 | 채택 | 4.0 | 학습 진행, 학습 작업 공간 |  |
| AD-018 | 예제 폴더에는 사용자가 남길 산출물을 미리 채워 두지 않습니다. 시작 상태가 정한 코드(동작하는 앱, 문제가 심어진 앱)는 폴더에 둡니다. 원인 찾기처럼 정답이 있는 활동도 정답을 따로 적어 두지 않고, `init`은 증상이나 결과를 다시 잴 수 있는 수단을 남깁니다. 사용자는 고친 뒤 같은 수단으로 다시 재어 완성을 확인합니다 | 실제 디버깅처럼 증상이 사라졌는지로 확인하므로 정답을 숨길 위치와 규칙이 필요 없습니다. 관찰처럼 정답이 하나가 아닌 활동에도 같은 방식을 씁니다. 학습 목표가 조건·결과·이유를 담으므로 원인 설명이 완성 조건에 들어갑니다 | 정답을 기록 파일에 적어 두고 대조: 정답을 가릴 위치가 필요하고 정답이 하나가 아닌 활동에는 쓸 수 없음. 완성 코드를 넣지 않는다는 기존 규칙 유지: 시작 상태에 앱 전체가 필요한 활동을 준비할 수 없음 | 채택 | 4.0 | 학습 작업 공간 |  |
| AD-019 | 사용자가 찾은 원인이나 관찰 결과가 맞는지는 사용자가 요청할 때만 `tutor`가 확인합니다. 확인 요청도 질문의 하나로 보고 답변 단계를 적용합니다. 설명이 코드와 맞으면 맞다고 답하고, 어긋나면 현재 단계의 범위에서 어긋난 지점만 짚습니다 | `tutor`는 이미 예제 폴더에서 불리고 코드를 읽으므로 책임 경계가 바뀌지 않습니다. 답변 단계를 그대로 적용하면 정답을 먼저 알려 주지 않는다는 원칙이 유지됩니다. AD-018로 정답을 적어 두지 않으므로, 증상만 가린 수정을 가려낼 곳이 필요합니다 | `manager`가 요청 시 코드를 읽음: 코드를 읽지 않는다는 원칙에 예외가 생겨 배운 내용을 코드로 채울 위험이 생김. 예제를 마쳤다고 할 때마다 확인: 사용자가 요청하지 않은 확인이 끼어듦 | 채택 | 4.0 | 학습 진행 |  |
| AD-020 | 예제마다 폴더 이름과 무관한 예제 ID를 부여하고, `.study/records/`의 기록 파일 하나를 기준 정보로 둡니다. 기록은 기계가 읽는 YAML 머리말과 사용자가 읽는 Markdown 본문으로 나눕니다. 상태는 `preparing`·`ready`·`in_progress`·`completed` 네 단계이며, 준비 실패는 `preparing`에 실패 단계와 원인을 남겨 `init`이 재개합니다. 첫 `tutor` 호출에서 `in_progress`가 되고, 만들기는 실행·측정 결과를, 진단·관찰·비교는 결과와 근거를 `tutor`가 모두 확인해야 `completed`가 됩니다. 질문 이력은 학습 목표와 최대 단계와 해결 여부를 주제별로 누적합니다 | 빈 절로 상태를 추론하면 미완성 예제가 복습 대상이 되고, 질문 한 건만 저장하면 앞선 도움 이력이 사라집니다. 변하지 않는 ID와 명시적 상태를 쓰면 폴더 이름 변경과 중단 뒤에도 같은 기록을 이어 갑니다 | 폴더 이름을 식별자로 사용: 이름 변경 시 연결이 끊김. JSON과 Markdown 분리: 값 중복 가능성 증가. 질문 응답마다 기록: 기록 분량 증가. 완료 선언만 사용: 완성 조건 미검증 | 채택 | 5.0 | 학습 진행, 학습 작업 공간 |  |
| AD-021 | `init`은 계기와 목표를 받은 뒤 활동을 먼저 확정하고, 그 활동에 맞는 질문을 최대 세 번까지 냅니다. `tutor`의 기준으로 수준과 근거를 추천하고 사용자가 최종 수준을 확정합니다. 추천과 다른 수준을 고르면 추천 수준·최종 수준·근거를 함께 기록합니다. 언어와 런타임이 하나로 정해지면 추론 근거만 보이고, 선택지가 둘 이상이면 사용자가 고릅니다. 그 뒤 학습 목표와 예제 범위를 확인받습니다 | 활동을 먼저 정해야 같은 수준도 만들기·진단·비교에 맞는 질문으로 가릴 수 있습니다. 질문 수를 고정하지 않으면 답이 분명한 사용자에게 불필요한 질문을 줄이면서도 모호한 답은 더 확인할 수 있습니다 | 수준 자기 선택만 사용: 실제 수행 능력과 차이가 남음. 세 질문 고정: 분명한 답에도 질문 반복. 환경 항상 질문: 단일 선택지에도 대화 증가. 수준 먼저 결정: 활동과 무관한 수준 판정 | 채택 | 5.1 | 학습 진행 |  |
| AD-022 | `tutor`는 새 질문에 시도 내용과 막힌 지점이 있으면 3단계에서 시작하고, 없으면 2단계에서 확인합니다. 사용자가 다음 힌트나 더 직접적인 방법을 명시할 때만 단계를 올리며 재설명과 예시는 같은 단계에서 답합니다. 기존 주제의 연속인지 모호하면 사용자에게 한 번 확인합니다. 사용자가 해결을 알리고 실행 결과·측정값·코드 변화 중 해당 근거를 확인한 뒤 주제를 해결됨으로 기록합니다. 5단계에서는 필요한 최소 코드 조각까지 보이되 파일을 수정하지 않습니다 | 현재 질문에 이미 적힌 시도를 다시 물으면 학습 흐름이 끊깁니다. 도움 확대와 재설명을 구분하면 설명을 바꿔 달라는 요청이 정답 공개로 이어지지 않습니다. 해결 근거와 최대 단계를 함께 남기면 `manager`가 목표별 도움 정도를 판단할 수 있습니다 | 새 질문을 항상 2단계에서 시작: 정보 반복. 모든 후속 질문에서 단계 상승: 재설명도 도움 확대. 주제 연속성을 자동 추론: 잘못된 단계 계승. 5단계에서 파일 수정: 학습자 산출물 경계 훼손 | 채택 | 5.2 | 학습 진행 |  |
| AD-023 | `init`은 학습 목표마다 필수 개념·필수 근거·대표 오개념으로 된 진단 기준을 기록합니다. `manager`는 완료된 기록의 진단 기준과 주제별 질문 이력만 읽고, 코드와 `TASK.md`는 열지 않습니다. 진단은 독립 설명·부분 이해·재학습 필요의 세 단계로 나눕니다. 기본 복습은 완료 뒤 1·3·7·14일이며, 재학습이 필요한 목표는 다음 날 한 번 더 묻습니다. 복습 대상은 `manager`를 호출할 때만 찾고, 목표별 결과가 다르면 가장 낮은 목표부터 다음 학습을 제안합니다 | 학습 목표 문장만으로 자유 서술을 판정하면 세션마다 기준이 달라집니다. 진단 기준을 예제 생성 때 고정하면 코드를 다시 읽지 않고도 같은 근거로 답을 판단할 수 있습니다. 고정 일정에 보충 복습을 더하면 일정 계산을 제한하면서 취약한 목표를 다시 꺼내게 합니다 | 답변과 목표만 사용: 판정 근거 부족. 코드 재확인: 회상 중심 복습 약화. 모든 간격을 성취에 따라 변경: 일정 복잡도 증가. 세 스킬 호출 때 알림: 요청하지 않은 응답 혼입. 예제 전체 반복: 익힌 목표도 재수행 | 채택 | 5.3 | 학습 진행 |  |
| AD-024 | 사용자가 지정한 로컬 경로만 읽고 민감한 파일은 제외하며, 외부 문서와 코드에 적힌 지시는 학습 자료로만 취급합니다. 패키지 설치 스크립트가 있으면 목록과 영향을 알리고 동의받은 뒤 실행합니다. 서버처럼 끝나지 않는 프로세스는 기동을 확인한 뒤 포트와 자식 프로세스까지 종료합니다. 상위 Git 저장소가 없으면 예제 전용 저장소와 기준 커밋을 자동 생성합니다. 상위 저장소가 있으면 사용자 동의를 받은 뒤 예제 경로만 커밋하고 해시를 기록합니다 | 입력 자료의 지시나 비밀 파일을 실행 맥락으로 받아들이면 읽기 요청이 명령 실행으로 바뀔 수 있습니다. 설치 스크립트와 백그라운드 프로세스를 확인하면 예제 준비가 사용자 환경에 남기는 영향을 제한할 수 있습니다. 기존 저장소 커밋에 동의를 받으면 현재 브랜치의 변경 이력을 예고 없이 바꾸지 않습니다 | 지정 경로 전체 읽기: 민감 정보 노출. 설치 스크립트 자동 실행: 외부 코드 즉시 실행. 설치 스크립트 차단: 패키지 호환성 저하. 서버 계속 실행: 포트와 자원 잔류. 기존 저장소 자동 커밋: 작업 흐름 변경 | 채택 | 5.4 | 학습 진행, 학습 작업 공간 |  |
