# <커밋 제목>

**커밋** — `<짧은 해시>` · <YYYY-MM-DD>

<!-- 본문의 서술 문장은 `~합니다`체로 씁니다. 표의 셀 값은 명사절입니다. -->

## 한 줄 요약

<!-- 이 커밋이 무엇을 바꿨는지 한 문장으로 씁니다. -->

## 변경 전 상태와 문제

<!-- 세 라벨을 각각 한 문장으로 적습니다. 문단으로 풀지 않습니다. -->

**이전 동작** — <직전 코드가 어떻게 동작했는가>

**문제** — <그 동작의 무엇이 문제였는가>

**실행 환경** — <그 코드가 어떤 실행 환경에서 쓰이도록 만들어졌는가>

## 변경 의도

<!-- 근거마다 한 줄인 목록으로 씁니다. 문단으로 풀지 않습니다. 근거를 찾지 못했으면 목록 대신 아래 표시만 남깁니다. -->

- [<항목 ID>](<계획서 경로>) — <그 항목이 정한 것>
- 커밋 메시지 — <작성자가 밝힌 목적>

<!-- 확인 필요: <무엇을 확인해야 하는가> -->

## 의존 관계

<!-- 방향은 TD로 고정하고 아래쪽에 상위 요소를 둡니다. 묶는 경계가 하나뿐이면 subgraph를 지웁니다. 구조를 재배치한 커밋이면 아래 두 블록을 모두 채우고, 그 밖의 커밋이면 변경 전 블록을 지웁니다. -->

**변경 전**

```mermaid
flowchart TD
    subgraph <경계 이름>[<패키지 또는 모듈>]
        A[<하위 구성 요소>]
    end
    subgraph <경계 이름>[<패키지 또는 모듈>]
        B[<상위 구성 요소>]
    end
    A --> B
```

**변경 후**

```mermaid
flowchart TD
    subgraph <경계 이름>[<패키지 또는 모듈>]
        A[<하위 구성 요소>]
    end
    subgraph <경계 이름>[<패키지 또는 모듈>]
        B[<상위 구성 요소>]
    end
    A --> B
```

<!-- 화살표마다 시작하는 쪽이 도착하는 쪽에 대해 무엇을 알아야 하는지 적습니다. 호출 여부만 적지 않습니다. 커밋이 건드리지 않은 인접 요소도 최소 하나 포함합니다. -->

| 화살표 | 알아야 하는 지식 |
| --- | --- |
| `<출발>` → `<도착>` | <출발 쪽이 도착 쪽에 대해 알아야 하는 것> |

## 구성 요소

<!-- 의존 관계와 실행 흐름에 나오는 코드 식별자를 소개합니다. 시그니처는 인용하지 않습니다. 식별자가 하나도 없으면 표에 해당 없음을 적습니다. -->

| 구성 요소 | 종류 | 어디에 | 역할 |
| --- | --- | --- | --- |
| `<이름>` | <타입 또는 함수 또는 설정> · <공개 또는 내부> | `<파일>` | <이 커밋의 맥락에서 맡는 것> |

## 실행 흐름

<!-- 입력부터 결과까지를 순서로 추적합니다. 단계마다 파일과 함수를 밝히고, 이 커밋으로 달라진 단계에는 표시를 붙입니다. -->

| 순서 | 단계 | 어디에서 | 변경 | 출처 |
| --- | --- | --- | --- | --- |
| 1 | <무엇이 일어나는가> | `<파일>`의 `<함수>` | <변경 없음 또는 동작 변경 또는 신규 추가> | <근거가 밝힌 경우에만 필수적 복잡성 또는 우발적 복잡성> |

## 용어와 배경

<!-- 뜻 열은 "무엇인가"에 답하는 명사구로 끝맺습니다. 서술형 문장으로 풀지 않습니다. -->

| 용어 | 뜻 |
| --- | --- |
| <본문에 나온 용어> | <이 커밋의 맥락에서 무엇을 가리키는가> |
