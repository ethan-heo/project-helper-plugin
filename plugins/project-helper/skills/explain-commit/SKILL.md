---
name: explain-commit
description: 이미 커밋된 변경을 이해하기 위해, 커밋 이력을 받아 커밋 하나마다 학습용 기술 문서 한 편을 만든다. 변경 전 상태와 변경 의도, 실행 흐름, 의존 관계 다이어그램, 용어 보충을 고정된 절 순서로 담아 코드를 다시 열지 않고도 그 커밋을 설명할 수 있게 한다. "이 커밋 이해하고 싶어", "커밋 설명 문서로 정리해줘", "어제 작업한 커밋들 공부하게 문서로 만들어줘", "/explain-commit" 같은 요청에서 사용한다. 계획서대로 구현하는 일에는 사용하지 않는다 — 그때는 impl-plan이다.
---

# explain-commit

## 역할과 목적

`explain-commit`은 사용자가 지정한 커밋을 읽어, 그 변경을 이해할 수 있는 학습 문서로 남깁니다.

AI는 조사와 서술을 맡고, 무엇을 배울지 고르는 일은 사용자가 합니다.

## 실행 원칙

**커밋에서 확인한 사실만 쓰고, 확인하지 못한 내용은 추측으로 채우지 않습니다.**

**호출 후 입력은 [스킬 실행 맥락](../_shared/session.md#입력-해석)에 따라 해석합니다.**

산출물은 대상 커밋마다 문서 한 편입니다. 한 문서가 두 커밋을 담지 않고, 한 커밋이 두 문서로 갈리지도 않습니다.

## 참조 문서 규칙

이 문서가 `explain-commit`의 실행 지시사항을 정하는 기준 문서입니다. 작업을 시작할 때 이 문서를 먼저 읽고, 아래 표의 절차에 진입할 때 지정된 참조 문서만 읽습니다.

| 문서 유형 | 역할 | 적용 범위 |
| --- | --- | --- |
| [`session.md`](../_shared/session.md) | 후속 입력 해석 | 호출 직후 |
| `SKILL.md` | 실행 절차와 분기 | `explain-commit` 전체 |
| `references/collecting.md` | 커밋 지정 형식과 중복·오류 판정 | 대상 수집과 목록 확인 |
| `references/document.md` | 문서의 절 구성과 설명 깊이 | 문서 생성 |
| `references/writing.md` | 서술 근거와 보관 경로·파일명 | 목록 확인·조사·문서 생성 |
| `../_shared/writing-style.md` | 산출물의 문체와 표기 | 문서 생성 |
| `../_shared/docs-root.md` | 문서 루트 판정 규칙 | 보관 경로 결정 |
| `assets/commit-doc-template.md` | 학습 문서 양식 | 문서를 만들 때 |
| `scripts/validate-commit-doc.sh` | 필수 절과 파일명 검증 | 결과 보고 직전 |

참조 문서는 본문에 없는 실행 범위를 추가하지 않습니다.

## 실행 절차

### 1. 대상을 수집합니다

**[`collecting.md`](references/collecting.md)를 읽고, 인자로 받은 커밋 지정을 커밋 목록으로 펼칩니다.**

입력 형식과 사전 검사는 [대상 수집](references/collecting.md#입력-형식)의 절차를 따릅니다.

### 2. 목록을 확인받습니다

**[보관 경로](references/writing.md#보관-경로)를 정한 뒤 [목록 확정 절차](references/collecting.md#목록을-확정하는-순서)에 따라 확인받습니다.**

### 3. 커밋마다 조사합니다

**[서술 근거](references/writing.md#서술-근거)에 따라 자료를 모으고, 근거가 없는 내용은 [확인 필요 표시](references/writing.md#확인-필요-표시)로 남깁니다.**

### 4. 문서를 만듭니다

**[`document.md`](references/document.md)의 절 구성으로 쓰고, [`writing.md`](references/writing.md)의 서술 규칙과 [`writing-style.md`](../_shared/writing-style.md)의 문체·표기 규칙을 적용합니다.**

`assets/commit-doc-template.md`를 복사해 일곱 절을 채웁니다. 문서를 만든 뒤 `scripts/validate-commit-doc.sh`로 형식을 검사합니다.

### 5. 결과를 보고합니다

**만든 문서, 건너뛴 커밋, 실패한 커밋을 나누어 알립니다.**

| 구분 | 알릴 내용 |
| --- | --- |
| 만든 문서 | 커밋의 짧은 해시와 생성된 파일 경로 |
| 건너뛴 커밋 | 짧은 해시와 이미 있는 문서의 경로 |
| 실패한 커밋 | 짧은 해시와 어느 단계에서 멈췄는지 |

## 예외 처리

입력 오류·중복·커밋별 생성 실패는 [대상 수집의 예외 처리](references/collecting.md#예외-처리)를 따릅니다.

## 경계

- 계획서대로 코드를 고치는 일은 `/impl-plan`입니다.
- 이 스킬은 코드를 고치지 않습니다.
- 코드의 품질을 평가하거나 개선을 제안하지 않습니다. 설명만 합니다.
- 커밋되지 않은 변경은 대상으로 삼지 않습니다.
- 이미 만든 문서는 코드가 바뀌어도 갱신하지 않습니다. 그 커밋 시점의 기록이기 때문입니다.
- 사용자의 이해도를 묻거나 채점하지 않습니다.
