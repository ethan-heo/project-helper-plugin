# <도메인 이름> — 도메인 모델

## 엔티티와 값 객체

```mermaid
erDiagram
    ENTITY_A ||--o{ ENTITY_B : "<관계 이름>"
    ENTITY_A {
        string id
        string <필드>
    }
```

| 이름 | 구분 | 역할 |
| --- | --- | --- |
| <이름> | 엔티티 / 값 객체 | <무엇을 표현하는가> |

### 생명 주기

<엔티티가 언제 생기고 어떤 상태를 거쳐 언제 사라지는지 쓴다. 상태 값이 있으면 전이 조건을 함께 적는다.>

## 데이터베이스 스키마

### `<테이블명>`

| 필드 | 타입 | 제약 조건 | 설명 |
| --- | --- | --- | --- |
| `id` | <타입> | PK | <설명> |
| `<필드>` | <타입> | <NOT NULL, UNIQUE, FK 등> | <설명> |

<이 도메인이 독점적으로 관리하는 테이블만 적는다. 다른 도메인의 테이블을 참조한다면 어떤 방식으로 참조하는지만 밝힌다.>

### 인덱스와 제약

| 대상 | 종류 | 이유 |
| --- | --- | --- |
| `<컬럼>` | <인덱스 / 유니크 / 외래 키> | <왜 필요한가> |

<!-- 미확인: <무엇을 확인하지 못했는가> -->
