# <도메인 이름> — API 명세

## 엔드포인트 목록

| 메서드 | 경로 | 설명 | 인증 |
| --- | --- | --- | --- |
| `POST` | `/api/<경로>` | <무엇을 하는가> | <필요 / 불필요> |

## `<메서드> <경로>`

<이 엔드포인트가 하는 일과 호출 조건을 한두 문장으로 쓴다.>

### 요청

| 위치 | 이름 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- | --- |
| body | `<필드>` | <타입> | 예 / 아니오 | <설명> |

```json
{
  "<필드>": "<예시 값>"
}
```

### 응답

```json
{
  "<필드>": "<예시 값>"
}
```

### 에러 코드

| 상태 코드 | 코드 | 발생 조건 |
| --- | --- | --- |
| 400 | `<코드>` | <어떤 입력일 때> |
| 409 | `<코드>` | <어떤 상태일 때> |

<공통 에러 코드는 여기에 옮겨 적지 않고 `공통_인프라_및_컨벤션/공통_에러_코드.md`를 참조한다.>

## 이벤트 메시징

<메시지 브로커를 쓰지 않으면 "해당 없음"과 그 이유를 적고 아래 표를 지운다.>

### 발행

| 토픽 | 발행 시점 | 메시지 스키마 |
| --- | --- | --- |
| `<토픽명>` | <어떤 일이 일어났을 때> | <아래 예시 참조> |

```json
{
  "eventId": "<식별자>",
  "occurredAt": "<ISO 8601>",
  "payload": {}
}
```

### 구독

| 토픽 | 처리 내용 | 실패 시 |
| --- | --- | --- |
| `<토픽명>` | <무엇을 하는가> | <재시도 / 보류 / 무시> |

<!-- 미확인: <무엇을 확인하지 못했는가> -->
