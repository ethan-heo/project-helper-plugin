# 인증 및 인가 가이드

## 인증 방식

<어떤 수단으로 사용자를 식별하는지 쓴다. 토큰을 쓴다면 발급·갱신·만료 규칙을 함께 적는다.>

| 항목 | 내용 |
| --- | --- |
| 방식 | <세션 / JWT / OAuth 등> |
| 발급 경로 | `<엔드포인트>` |
| 유효 기간 | <값> |
| 갱신 방법 | <어떻게> |

### 토큰 구조

```json
{
  "sub": "<식별자>",
  "exp": 0,
  "<클레임>": "<값>"
}
```

## 인가 규칙

| 역할 | 권한 범위 |
| --- | --- |
| <역할 이름> | <무엇을 할 수 있는가> |

### 엔드포인트 보호

<인증이 필요한 경로를 어떻게 지정하는지, 예외 경로가 무엇인지 쓴다.>

| 경로 패턴 | 요구 조건 |
| --- | --- |
| `<패턴>` | <인증 불필요 / 특정 역할 필요> |

## 실패 처리

| 상황 | 상태 코드 | 코드 |
| --- | --- | --- |
| 토큰 없음 | 401 | <코드> |
| 토큰 만료 | 401 | <코드> |
| 권한 부족 | 403 | <코드> |

<코드 값은 [`공통_에러_코드.md`](공통_에러_코드.md)와 일치시킨다.>

<!-- 미확인: <무엇을 확인하지 못했는가> -->
