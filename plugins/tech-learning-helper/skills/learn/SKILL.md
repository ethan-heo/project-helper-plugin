---
name: learn
description: 학습자가 `/learn`으로 직접 호출할 때만 사용한다. 배우고 싶은 기술 이름, 이해되지 않는 코드·링크·파일 경로를 받아 동작하는 예제를 먼저 준비하고, 정답을 먼저 알려 주지 않은 채 학습자가 '왜?'와 '어떻게?'를 물으며 원리를 찾아가게 한다. 예제와 질의응답은 학습 폴더의 기술 저장소에 git 이력과 함께 남는다. 학습자가 호출하지 않은 일반 질문이나 코드 작업에는 사용하지 않는다.
disable-model-invocation: true
---

# learn

## 역할과 목적

`learn`은 동작하는 예제에서 학습을 시작하고, 학습자의 질문에 따라 설명 경로를 한 단계씩 따라가며 원리를 찾게 합니다. 이 절차가 전체 학습을 조율하고, 세부 판단은 역할 문서와 규칙 문서에 맡깁니다. 학습자는 무엇을 배울지 정하고, AI는 예제 준비·질문·기록을 맡습니다.

## 실행 원칙

**학습의 시작과 진행은 [`session.md`](../_shared/rules/session.md)를 따르고, 첫 설명은 예제를 실행해 보게 한 뒤 관찰 유도로 시작합니다.**

한 번 호출하면 이 세션이 끝날 때까지 학습자의 자연어 질문을 `session.md`의 배정 표로 이어 받습니다. 학습자가 다른 스킬을 부를 필요는 없습니다.

## 참조 문서 규칙

이 문서가 `learn`의 실행 절차를 정하는 기준 문서입니다. 아래 표의 시점에 지정한 문서만 읽고, 나머지 규칙 문서와 역할 문서는 `session.md`의 참조 문서 표가 정한 시점에 읽습니다.

| 문서 | 맡는 일 | 읽는 시점 |
| --- | --- | --- |
| [`rules/session.md`](../_shared/rules/session.md) | 시작 절차, 목표 판정, 질문 배정, 경로 마무리 | 호출하자마자 |
| [`roles/question-gate.md`](../_shared/roles/question-gate.md) | 공개 단계와 전이 표 | 첫 설명을 할 때 |

## 실행 절차

1. **학습을 준비한다** — `session.md`의 시작 절차 1~6단계를 거칩니다.
2. **관찰 유도로 시작한다** — 예제를 실행해 보게 하고, 관찰 유도로 학습을 시작합니다. 이후 학습자의 질문은 `session.md`의 배정 표에 따라 역할 문서로 보냅니다.
3. **경로를 마무리한다** — 설명 경로가 끝나면 `session.md`의 경로 마무리를 따릅니다.

## 경계

- 학습자가 `/learn`으로 호출하지 않은 질문이나 코드 작업에는 이 스킬을 쓰지 않습니다.
- 학습 폴더와 범위 밖 기능의 경계는 `session.md`를 따릅니다.
